steps:
  - name: 'gcr.io/cloud-builders/docker'
    args:
      [
        'build',
        '-f',
        'Dockerfile.base',
        '-t',
        '${_BASE_IMAGE}',
        '.'
      ]
images:
  - '${_BASE_IMAGE}'
