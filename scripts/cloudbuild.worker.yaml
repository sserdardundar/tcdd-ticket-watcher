steps:
  - name: 'gcr.io/cloud-builders/docker'
    args:
      [
        'build',
        '-f',
        'Dockerfile.worker',
        '-t',
        '${_REGION}-docker.pkg.dev/$PROJECT_ID/${_REPO}/tcdd-worker:latest',
        '.'
      ]
images:
  - '${_REGION}-docker.pkg.dev/$PROJECT_ID/${_REPO}/tcdd-worker:latest'
